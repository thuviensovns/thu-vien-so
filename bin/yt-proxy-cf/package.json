{
  "name": "yt-proxy-cf",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker YouTube proxy — runs on CF's global edge, free tier",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
